import React from 'react';
import Form from './Form';
import DataDisplay from './DataDisplay';

const Post = () => {
  return (
    <div>
      <h2>Knowledge Sharing App</h2>
      <Form />
      <DataDisplay />
    </div>
  );
};

export default Post;
